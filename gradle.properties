# Per-module versions. Each module is published to Maven Central
# independently — releases are cut as `core-vX.Y.Z` and `mca-vX.Y.Z`
# tags handled by .github/workflows/publish-core.yml and publish-mca.yml.
coreVersion=0.1.1-SNAPSHOT
mcaVersion=0.2.0-SNAPSHOT
