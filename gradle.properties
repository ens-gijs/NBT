# Per-module versions. Each module is published to Maven Central
# independently — releases are cut as `nbt-vX.Y.Z` and `mca-vX.Y.Z`
# tags handled by .github/workflows/publish-nbt.yml and publish-mca.yml.
nbtVersion=0.1.1
mcaVersion=0.2.1-SNAPSHOT
